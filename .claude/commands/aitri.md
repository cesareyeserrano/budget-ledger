---
description: Advance the Aitri pipeline — one step
---
Run `aitri resume`. Read the output and follow the PIPELINE INSTRUCTION at the end exactly —
it names the single next action. Do not choose an alternative, skip phases, or re-open
approved phases. If the instruction is a `run-phase`, read the briefing it prints and produce
the artifact at the path it specifies, then run the `complete` it names.
